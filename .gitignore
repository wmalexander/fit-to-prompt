# macOS
.DS_Store

# Claude
CLAUDE.md

# Process files (used by /check-sources and /build-newsletter)
last-check.txt
next-newsletter-content.md
sources.md
newsletter-layout-playground.html

# Pipeline lock (local-only, never commit)
pipeline.lock

# Buttondown email drafts (managed via API)
buttondown/
